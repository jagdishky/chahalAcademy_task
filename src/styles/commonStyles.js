
import { StyleSheet } from "react-native";

const commonStyle = StyleSheet.create({
    flexDirectionRow: {
        flexDirection: "row",
        alignItems: "center"
    },
})

export default commonStyle